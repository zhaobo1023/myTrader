name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  deploy:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect frontend changes
        id: changes
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q '^web/'; then
            echo "web=true" >> $GITHUB_OUTPUT
          else
            echo "web=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ECS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.ECS_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Deploy to ECS
        # deploy_remote.sh 使用 APP_DIR=/app/myTrader（容器 volume 挂载目录）
        # 前端蓝绿切换由脚本自动完成，无需额外参数
        env:
          WEB_CHANGED: ${{ steps.changes.outputs.web }}
        run: |
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            scripts/deploy_remote.sh \
            ${{ secrets.ECS_USER }}@${{ secrets.ECS_HOST }}:/tmp/deploy_remote.sh

          ssh -i ~/.ssh/deploy_key -o BatchMode=yes -o StrictHostKeyChecking=no \
            ${{ secrets.ECS_USER }}@${{ secrets.ECS_HOST }} \
            "WEB_CHANGED=${WEB_CHANGED} bash /tmp/deploy_remote.sh"
